apiVersion: v1
kind: Service
metadata:
  name: backend             # MUST be "backend": the frontend nginx proxies /api -> backend:5000
  namespace: taskapp
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
