# TEMPLATE / REFERENCE ONLY — never apply or commit real values. Real Secret comes from create-secret.sh.
apiVersion: v1
kind: Secret
metadata:
  name: taskapp-secret
  namespace: taskapp
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME_random_hex"
  POSTGRES_PASSWORD: "REPLACE_ME_strong_password"
  DATABASE_PASSWORD: "REPLACE_ME_same_password"     # Alembic entrypoint reads this
  DATABASE_URL: "postgresql://taskuser:REPLACE_ME_same_password@postgres:5432/taskmanager"
