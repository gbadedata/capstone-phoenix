apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: taskapp
  labels:
    app: postgres
spec:
  clusterIP: None          # headless: governs the StatefulSet pods
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
